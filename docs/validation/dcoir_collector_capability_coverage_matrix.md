# DCOIR Collector Capability Coverage Matrix

This source-maintained matrix is the human review companion for the harness-generated `collector_capability_coverage.json` and `collector_capability_coverage.md` artifacts. It exists to keep #187 from becoming an ad hoc pass/fail judgment: every advertised collector capability should either have strong FullRegression/manual-full-validation evidence, a deliberate synthetic-only boundary, a justified out-of-scope boundary, or a tracked gap.

## Current High-Confidence Coverage

| Capability | Current Evidence | Status |
| --- | --- | --- |
| Core collect output contract | FullRegression `Core` suite: collect contract and attachment budget checks | Covered when current-head FullRegression passes |
| Basic enrich session lifecycle | Core/session suites: start, add, finalize, reuse, and finalized output contract checks | Covered when current-head FullRegression passes |
| Enrich session lifecycle edge cases | SessionBehavior suite: finalize-without-open rejection, explicit session reuse, force-new session creation, requested-session finalization, finalized-session append rejection, open-session finalize, add-after-finalize new session creation, missing requested session rejection, and cleanup while an enrich session is open | Covered when current-head FullRegression passes |
| Valid targeted explicit window preservation | Targeted suite exact `WindowStart` / `WindowEnd` assertions in scope of this #187 batch | Covered when current-head FullRegression passes |
| Invalid targeted window fallback | FailureGates invalid start, invalid end, inverted window, paired validation rows | Covered when current-head FullRegression passes |
| Synthetic chunking byte reconstruction | ChunkingOversizeArtifact and ChunkingReconstructionMetadata suites | Covered when current-head FullRegression passes |
| Production event-text chunk reconstruction | Security, PowerShell Operational, and Task Scheduler Operational deterministic oversize path in scope of this #187 batch | Covered when current-head FullRegression passes |
| Bounded Tier 2 collection | `Tier2BoundedCollect` suite invokes `-Tier T2 -Hours 1 -MaxEvents 100`, checks standard collect contract, verifies `Tier=T2`, `Hours=1`, and `MaxEvents=100` metadata, and verifies Tier 2 deep-check artifacts in the bundle | Covered when current-head FullRegression passes |
| Missing package diagnostics and no-state cleanup | FailureGates missing package checked paths, orphan cleanup, plain cleanup preservation of custom RunId-like no-state and state-backed roots, and explicit custom RunId no-state cleanup | Covered when current-head FullRegression passes |
| Durable harness progress evidence | `progress.jsonl` and `progress.txt` generated by harness in scope of this #187 batch | Covered when current-head FullRegression passes |
| Retained evidence artifacts | Harness `evidence/` copies for response path artifacts/chunks in scope of this #187 batch | Covered when current-head FullRegression passes |
| Capability coverage artifact | Harness generated JSON and Markdown coverage artifacts in scope of this #187 batch | Covered when current-head FullRegression passes |
| Operator knowledge source alignment | Indexed knowledge pages were updated for session guardrails, bounded Tier 2 validation, source-backed quick aliases, upload-safe chunks, cleanup fallback boundaries, `MaxEvents`, and artifact review order; documentation-quality rejects misattached function help blocks | Requires separate doc/readback review receipt; FullRegression does not prove knowledge-page completeness |

## Dispositioned Follow-on Validation Targets

These items are not treated as proven by the current #187/#186 FullRegression surface. They remain valuable additive validation work, but they have been split into explicit child tracking issues so the current PR does not overclaim them.

| Gap | Risk | Disposition |
| --- | --- | --- |
| Full TargetProfile matrix | Medium/high | Tracked in #192 for additive assertions covering `Generic`, `ScriptExecution`, `PersistenceFollowUp`, `NetworkOnly`, and `ProcessAndPowerShell` profile-specific guidance, focus fields, included categories, and handoff artifacts. |
| Broader quick alias artifact contracts | Medium | Tracked in #192 for additive per-alias target echo, artifact class, session inclusion, and bounded no-data diagnostic assertions. |
| Raw EVTX exact event-window inclusion/exclusion | High where deterministic fixtures are possible | Tracked in #193 for fixture-backed or abstraction-backed raw EVTX include/exclude tests. Current #186/#187 evidence must not be described as raw EVTX content proof when it only proves metadata, scope, fallback, or event-text behavior. |

## Historical Deep-Gate Deficiency Backlog

Codi and Adva deep review on PR head `69874498e760a1f884a4ee3477a7600ffd94beca` did not clear the deep PR/subordinate-issue gate. These rows are historical backlog evidence carried forward into the current head; they are not a current-head Codi/Adva approval or rejection by themselves.

| Reviewer | Finding | Current disposition | Required action |
| --- | --- | --- | --- |
| Codi | P1: manual validation evidence is missing for the current head. | Still open for the active PR head. Current automatic checks do not replace current-head manual FullRegression, collector runtime package build, Gemini bundle build, or post-draft `validate-on-pr`. | Run/read back the manual workflows on the PR branch and record run IDs, head SHA, job/step outcomes, and artifacts before closeability/readiness claims. |
| Codi | P2: #192 and #193 are not satisfied child requirements. | Still open as an explicit follow-on-scope disposition. | Keep #192/#193 out of completed-scope claims unless the operator explicitly accepts them as follow-on work with rationale, or implement and validate them. |
| Adva | P1: current-head source readback was not mapped to subordinate issue requirements. | Addressed by the source-readback map below, subject to current-head review and external review readback. | Keep the source-readback map tied to the active PR head and rerun review gates after material source or matrix changes. |
| Adva | P1: current-head FullRegression evidence is missing. | Still open for the active PR head. | Run/read back `manual-full-validation` with `suite=FullRegression` on the PR branch. |
| Adva | P1: current-head collector runtime package build evidence is missing. | Still open for the active PR head. | Run/read back `manual-collector-runtime-package-build` on the PR branch. |
| Adva | P1: current-head Gemini bundle build evidence is missing. | Still open for the active PR head. | Run/read back `manual-gemini-bundle-build` on the PR branch with validation enabled. |
| Adva | P2: `validate-on-pr` remains skipped while the PR is draft. | Future gate. | Keep the PR draft until earlier gates pass; after draft lift, read back `validate-on-pr` for the lifted head. |

## Active Review Status

This section tracks review status for the active PR branch without hardcoding a live head SHA into the source file. The exact active head must be read back from GitHub PR metadata and, when recorded, the Supabase work-item receipt ledger.

| Review surface | Status | Remaining gap |
| --- | --- | --- |
| Focused 04G source-mode wrapper follow-up | Focused Prog/Adva/Codi passed; external `@codex` follow-up reported no major issues for the prior P2 on the then-current head. | This clears only the focused wrapper follow-up, not the final full PR gate or manual workflow gates. |
| Final end-to-end Codi split review | In progress after matrix stale-head wording fix. | Rerun Codi on the updated matrix and then complete external full PR review before manual workflow gates. |
| Manual workflow evidence | Not yet run/read back for the active PR head. | FullRegression, collector runtime package build, Gemini bundle build, and post-draft `validate-on-pr` remain required later gates. |

## Current-Head Source-Readback Map

This map records the governed source surfaces checked while working the Adva source-readback finding. It is anchored to the active PR branch, with the exact active head supplied by GitHub/Supabase readback instead of by this static file, and it does not replace workflow run evidence.

| Requirement area | Source surfaces read back | Evidence observed | Remaining gap |
| --- | --- | --- | --- |
| #70 exact event-window metadata and fallback | `project_sources/collector/source/parts/DCOIR_Collector.04C_Explicit_Event_Window_Overrides.ps1`; `project_sources/collector/source/parts/DCOIR_Collector.04F2_PR186_Review_Fixes.ps1`; `project_sources/collector/harness/run_DCOIR_Tests.ps1` | Effective event-window parsing, `Get-WinEvent` filter construction, `WINDOW_START`, `WINDOW_END`, `MAX_EVENTS` metadata, invalid/inverted fallback, rejected-bound clearing, and targeted validation steps are present. | Current-head FullRegression must pass the mapped targeted/failure-gate rows. Raw EVTX exact include/exclude proof remains #193 unless implemented here. |
| #125 package diagnostics and no-state cleanup | `project_sources/collector/source/parts/DCOIR_Collector.04F1_PR186_Review_Fixes.ps1`; `project_sources/collector/harness/run_DCOIR_Tests.ps1`; `.github/workflows/manual-collector-runtime-package-build.yml` | Exact custom `RunId` lookup is preserved; blank/latest cleanup stays timestamp-bounded; no-state cleanup requires collector-created child structure; package workflow validates/builds/uploads collector runtime artifacts. | Current-head manual collector runtime package build and FullRegression cleanup/failure-gate rows must be read back. |
| #126 upload-safe chunking | `project_sources/collector/harness/run_DCOIR_Tests.ps1`; Gemini `Sub_Agent_07_DCOIR_Collector_Artifact_Interpreter_and_Report_Extractor.md.txt` | Harness records `UPLOAD_SAFE_CHUNK_MANIFEST_PATH`, checks source/chunk SHA256 values, enforces per-file budget, reconstructs production event-text chunks, and copies evidence artifacts; Gemini artifact interpreter prevents manifest overclaiming. | Current-head FullRegression must pass production chunking and reconstruction rows. |
| #187 capability matrix and hang/progress evidence | `docs/validation/dcoir_collector_capability_coverage_matrix.md`; `project_sources/collector/harness/run_DCOIR_Tests.ps1`; `.github/workflows/manual-full-validation.yml` | Matrix now tracks high-confidence coverage, follow-on gaps, historical backlog, and active review status without hardcoding an active head SHA; harness emits `progress.jsonl`, `progress.txt`, retained evidence, and generated capability coverage artifacts. | Current-head FullRegression artifacts must be read back before #187 closeability. |
| #188 harness modularization | `project_sources/collector/harness/assemble_run_DCOIR_Tests.ps1`; `project_sources/collector/harness/run_DCOIR_Tests.ps1`; `project_sources/collector/harness/source/parts/*.ps1.txt` | Assembled harness keeps the suite selector surface including `FullRegression` and `Tier2BoundedCollect`; tail guard exits nonzero when any result row is `FAIL`. | Current-head package/manual validation must prove the assembled harness lane. |
| #189 workflow hardening | `.github/workflows/manual-full-validation.yml`; `.github/workflows/manual-collector-runtime-package-build.yml`; `.github/workflows/manual-gemini-bundle-build.yml`; `.github/workflows/validate-on-push.yml`; `.github/workflows/validate-on-pr.yml` | Manual FullRegression exposes `Tier2BoundedCollect`; manual workflows produce report-section artifacts; push/PR path filters include the governed root index and matrix surfaces. | Current-head manual run evidence and later post-draft `validate-on-pr` evidence remain required. |
| #190 Gemini alignment | Gemini `Sub_Agent_06_DCOIR_Collector_Execution_and_Bundle_Workflow_Orchestrator.md.txt`; Gemini `Sub_Agent_07_DCOIR_Collector_Artifact_Interpreter_and_Report_Extractor.md.txt`; `.github/workflows/manual-gemini-bundle-build.yml` | Sub-Agent 06 anchors collector syntax to governed source, preserves command-lane boundaries, targeted window limitations, `MaxEvents`, upload-safe chunk availability, and custom `RunId` cleanup. Sub-Agent 07 distinguishes manifests from evidence carriers and handles upload-safe chunk review order. | Current-head Gemini bundle build/readback remains required before #190 closeability/readiness claims. |
| #192/#193 follow-on targets | `docs/validation/dcoir_collector_capability_coverage_matrix.md`; issues #192/#193 | The matrix keeps TargetProfile/quick-alias validation and raw EVTX exact include/exclude proof as explicit follow-on targets. | Operator disposition or separate implementation/validation is required before claiming those requirements satisfied. |

## Closeability Boundary

#187 should close only after current-head manual FullRegression evidence is read back for the current PR scope and the remaining follow-on validation targets are either completed, accepted as separately tracked follow-on work, or otherwise dispositioned by the operator with explicit rationale.

Manual FullRegression can prove the current PR harness rows, progress artifacts, production chunking rows, Tier 2 bounded collect rows, cleanup rows, and capability coverage artifacts. It does not by itself prove the follow-on #192/#193 validation targets or operator knowledge-page completeness.
