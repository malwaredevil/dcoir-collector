# Agent Runtime Test Artifacts

Provider-neutral test artifacts for behavior shared across governed agent runtimes.

Use capability-oriented directories here when the same operator-visible contract applies to more than one target, such as Gemini Agent sub-agents and OpenAI GPTs. Provider-specific topology may differ; the shared fixture should exercise the common external behavior.
