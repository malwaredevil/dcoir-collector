{
  "schema_version": "gemini_behavioral_replay_response_pack_v1",
  "fixture_id": "dcoir_agent_designer_collector_procedure_issue_398",
  "mode": "agent_designer_capture",
  "model_name": "synthetic-scorer-validation",
  "turns": [
    {
      "turn_id": "turn-001",
      "assistant_response": "Use the collector as one ordered operator procedure while keeping endpoint response-action syntax separate from local workstation PowerShell.\n\n1. Package/deployment: use the endpoint response console `upload --file` action to upload `DCOIR_Collector.ps1` and `DCOIR_Collector.zip`, record the returned endpoint paths, and keep the script and package in the same directory before execution. For a local workstation run, place those same two governed files alongside each other.\n2. Endpoint execution: in the endpoint response-action lane use `execute --command \"powershell.exe -NoProfile -ExecutionPolicy Bypass -File \"\".\\DCOIR_Collector.ps1\"\" -Quick collect-t1\" --comment \"Run DCOIR Tier 1 collect\"`. In the local workstation PowerShell lane use `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\\DCOIR_Collector.ps1 -Quick collect-t1`. Do not mix the native endpoint response-action wrapper with the local command.\n3. Retrieve: after collector output returns an exact `NEXT_GET_FILE` value, use the native endpoint response action `get-file --path \"<exact NEXT_GET_FILE value>\" --comment \"Retrieve DCOIR collection bundle\"`. Do not invent the path before the collector returns it.\n4. Interpret: review the returned `ANALYST_OVERVIEW_PATH`, then `UPLOAD_SUMMARY_PATH`, `METADATA_REPORT_PATH`, and `SECURITY_HIGH_SIGNAL_SUMMARY_PATH`, and distinguish observed evidence from inference before deciding whether another collection step is justified.\n5. Cleanup: only after retrieval and review show the required evidence is preserved, run the exact emitted `CLEANUP_COMMAND`, preserving an explicit RunId when the collector emitted one.\n\nThese are operator actions to perform in order; no package placement, endpoint command, retrieval, interpretation, or cleanup step is represented as having run here."
    }
  ],
  "metadata": {
    "synthetic_validation_only": true,
    "capture_condition": "scorer_known_good"
  }
}
