{
  "schema_version": "gemini_behavioral_replay_response_pack_v1",
  "fixture_id": "dcoir_agent_designer_collector_procedure_issue_398",
  "mode": "agent_designer_capture",
  "model_name": "synthetic-scorer-validation",
  "turns": [
    {
      "turn_id": "turn-001",
      "assistant_response": "Use the collector as one ordered operator procedure while keeping endpoint response-action syntax separate from local workstation PowerShell.\n\n1. Package/deployment: use the endpoint response console `upload --file` action to upload `DCOIR_Collector.ps1` and `DCOIR_Collector.zip`, record the returned endpoint paths, and keep both files in the same directory.\n2. Endpoint execution: use `execute --command \"powershell.exe -NoProfile -ExecutionPolicy Bypass -File \"\".\\DCOIR_Collector.ps1\"\" -Quick collect-t1\" --comment \"Run DCOIR Tier 1 collect\"` for the endpoint response-action lane. For local workstation PowerShell use `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\\DCOIR_Collector.ps1 -Quick collect-t1`; do not mix the lanes.\n3. Retrieve: after collector output returns `NEXT_GET_FILE`, use `get-file --path \"<exact NEXT_GET_FILE value>\" --comment \"Retrieve DCOIR collection bundle\"`.\n4. Evidence review: read `ANALYST_OVERVIEW_PATH`, `UPLOAD_SUMMARY_PATH`, `METADATA_REPORT_PATH`, and `SECURITY_HIGH_SIGNAL_SUMMARY_PATH` in that order and distinguish observations from inference.\n5. Cleanup: after required evidence is preserved, run the exact emitted `CLEANUP_COMMAND`, preserving any explicit RunId.\n\nThese are actions to perform; no step is represented as completed."
    }
  ],
  "metadata": {
    "synthetic_validation_only": true,
    "capture_condition": "scorer_known_bad_missing_stage_only"
  }
}
