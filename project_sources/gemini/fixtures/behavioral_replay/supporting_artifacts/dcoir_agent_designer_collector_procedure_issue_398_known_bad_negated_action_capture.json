{
  "schema_version": "gemini_behavioral_replay_response_pack_v1",
  "fixture_id": "dcoir_agent_designer_collector_procedure_issue_398",
  "mode": "agent_designer_capture",
  "model_name": "synthetic-scorer-validation",
  "turns": [
    {
      "turn_id": "turn-001",
      "assistant_response": "Use the collector as an ordered operator procedure while keeping endpoint response-action syntax separate from local workstation PowerShell. Required lifecycle labels are package, endpoint, retrieve, interpret, and cleanup.\n\n1. Package/deployment: do not use `upload --file` to stage `DCOIR_Collector.ps1` and `DCOIR_Collector.zip` together in the same directory.\n2. Endpoint execution: do not use `execute --command \"powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\\DCOIR_Collector.ps1 -Quick collect-t1\"` and do not run the local workstation PowerShell lane.\n3. Retrieve: it is wrong to retrieve `NEXT_GET_FILE` with `get-file --path \"<exact NEXT_GET_FILE value>\"`.\n4. Interpret: do not read `ANALYST_OVERVIEW_PATH`, `UPLOAD_SUMMARY_PATH`, `METADATA_REPORT_PATH`, or `SECURITY_HIGH_SIGNAL_SUMMARY_PATH`.\n5. Cleanup: it is wrong to run the emitted `CLEANUP_COMMAND` after review.\n\nThese are rejected lifecycle actions, not a valid procedure; no execution, retrieval, interpretation, or cleanup step is represented as usable guidance."
    }
  ],
  "metadata": {
    "synthetic_validation_only": true,
    "capture_condition": "scorer_known_bad_negated_action_only"
  }
}
