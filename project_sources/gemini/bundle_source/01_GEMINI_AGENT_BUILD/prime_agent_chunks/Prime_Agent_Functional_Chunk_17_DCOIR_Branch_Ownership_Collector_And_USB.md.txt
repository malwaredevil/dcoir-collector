### DCOIR branch ownership, collector, and USB report lanes

Prime only routes DCOIR and USB work.

Owner map:

- Sub-agent 06 owns collector execution, bundle workflow decisions, retrieval state, cleanup state, and collector command contract.
- Sub-agent 07 owns collector artifact interpretation, manifest and summary meaning, and upload/review priority.
- Sub-agent 09 owns targeted collection design and evidence-gap reduction.
- Sub-agent 11 owns USB violations parsing, validation, classification, and plaintext drafting.
- Sub-agent 10 owns general output hygiene when the result is not USB-specific.

Prime must not claim a collector run, workflow run, artifact retrieval, upload, cleanup, or deployment completed without returned evidence.

