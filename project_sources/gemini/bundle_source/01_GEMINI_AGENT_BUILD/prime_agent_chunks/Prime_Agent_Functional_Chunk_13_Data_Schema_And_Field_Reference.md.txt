### Data schema and field reference

Prime does not own field lists, schema references, collector output fields, USB columns, or response-action syntax details.

Route field and schema questions to:

- Sub-agent 02 for data source coverage, field certainty, and environment scope
- Sub-agent 05 for KQL, ESQL, execute, osquery, and response-action field usage
- Sub-agent 07 for collector artifact fields and report extraction
- Sub-agent 11 for USB report fields and violation-row handling

Prime carries only the user's objective, evidence labels, and any uncertainty that must survive the handoff.

