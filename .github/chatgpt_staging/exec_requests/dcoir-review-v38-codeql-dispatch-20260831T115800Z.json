{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "dcoir-review-v38-codeql-dispatch-20260831T115800Z",
  "operator_approved": true,
  "approved_at_utc": "2026-08-31T11:58:00Z",
  "shell": "pwsh",
  "timeout_seconds": 600,
  "artifact_retention_days": 3,
  "cleanup_request_after_run": true,
  "approved_command_preview": "Dispatch the repository's actual CodeQL Security workflow against current main using the authorized GitHub workflow token; do not modify reviewer source or PR branches.",
  "command": "$ErrorActionPreference = 'Stop'\n$token = $env:DCOIR_GITHUB_CL_TOKEN\nif ([string]::IsNullOrWhiteSpace($token)) { $token = $env:DCOIR_GITHUB_FG_TOKEN }\nif ([string]::IsNullOrWhiteSpace($token)) { throw 'No authorized GitHub token is available for workflow dispatch' }\n$headers = @{ Authorization = \"Bearer $token\"; Accept = 'application/vnd.github+json'; 'X-GitHub-Api-Version' = '2022-11-28' }\n$body = @{ ref = 'main' } | ConvertTo-Json -Compress\nInvoke-RestMethod -Method Post -Uri 'https://api.github.com/repos/malwaredevil/dcoir-collector/actions/workflows/codeql-security.yml/dispatches' -Headers $headers -ContentType 'application/json' -Body $body | Out-Null\nWrite-Host 'CodeQL workflow_dispatch requested for main'\n"
}
