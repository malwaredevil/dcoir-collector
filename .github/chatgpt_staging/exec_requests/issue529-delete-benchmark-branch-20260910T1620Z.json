{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "issue529-delete-benchmark-branch-20260910T1620Z",
  "operator_approved": true,
  "approved_at_utc": "2026-09-10T16:20:00Z",
  "shell": "pwsh",
  "timeout_seconds": 300,
  "artifact_retention_days": 3,
  "cleanup_request_after_run": true,
  "approved_command_preview": "Delete only the closed benchmark branch test-529-v56-controlled-replay after verifying its remote head is exactly 3d4ba4cc9f086a8c478649afba7c048331e1b7a0; fail closed if the branch moved; verify the remote ref is absent afterward. Do not modify main or agent-ops and do not invoke model inference.",
  "command": "$ErrorActionPreference = 'Stop'\n$branch = 'test-529-v56-controlled-replay'\n$expectedHead = '3d4ba4cc9f086a8c478649afba7c048331e1b7a0'\n$ref = \"refs/heads/$branch\"\n$before = @(git ls-remote --heads origin $ref)\nif ($LASTEXITCODE -ne 0) { throw 'Failed to read remote branch head.' }\nif ($before.Count -eq 0) { Write-Output \"Branch $branch is already absent.\"; exit 0 }\n$actualHead = (($before[0] -split '\\s+')[0]).Trim()\nif ($actualHead -ne $expectedHead) { throw \"Refusing deletion: $branch moved from expected $expectedHead to $actualHead.\" }\ngit push origin --delete $branch\nif ($LASTEXITCODE -ne 0) { throw \"git push --delete failed for $branch.\" }\n$after = @(git ls-remote --heads origin $ref)\nif ($LASTEXITCODE -ne 0) { throw 'Failed to verify remote branch deletion.' }\nif ($after.Count -ne 0) { throw \"Branch $branch still exists after deletion attempt.\" }\nWrite-Output \"Deleted and verified absent: $branch@$expectedHead\""
}