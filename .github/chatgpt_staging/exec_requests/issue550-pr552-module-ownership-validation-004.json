{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "issue550-pr552-module-ownership-validation-004",
  "operator_approved": true,
  "shell": "powershell_5",
  "timeout_seconds": 1800,
  "artifact_retention_days": 7,
  "cleanup_request_after_run": true,
  "approved_command_preview": "Corrected exact-head validation for PR #552 after request 003 proved the clean module-loader selftest passes but the harness treated the intentionally failing negative probe's stderr as terminating PowerShell error. Preserve the same approved validation goal and run expected-failure probes through cmd with stdout/stderr suppressed: exact head/base ancestry, clean detached worktree, secret removal, syntax check, clean selftest pass, unowned-module rejection, frozen-legacy-size growth rejection, restoration/recovery pass, PR-range diff check, clean worktree. No DCOIR Review, provider calls, PR mutation, Ready transition, or merge.",
  "command": "$ErrorActionPreference = 'Stop'; $target = '6d6e30bf53ed1b186fcd9699a7ae5b1550f3092a'; $base = '0273ac05445a469849f81588377c106e6e62a242'; $branch = 'fix/issue-550-module-ownership-validation'; git fetch --no-tags origin \"refs/heads/${branch}:refs/remotes/origin/${branch}\" \"refs/heads/main:refs/remotes/origin/main\"; if ($LASTEXITCODE -ne 0) { throw 'FETCH_FAILED' }; $remoteHead = (git rev-parse \"refs/remotes/origin/${branch}\").Trim(); if ($remoteHead -ne $target) { throw \"STALE_PR_HEAD:$remoteHead\" }; git merge-base --is-ancestor $base $target; if ($LASTEXITCODE -ne 0) { throw 'BASE_NOT_ANCESTOR_OF_TARGET' }; $worktree = Join-Path $env:RUNNER_TEMP 'issue550-pr552-exact-head-004'; if (Test-Path -LiteralPath $worktree) { Remove-Item -LiteralPath $worktree -Recurse -Force }; git worktree add --detach $worktree $target; if ($LASTEXITCODE -ne 0) { throw 'WORKTREE_ADD_FAILED' }; try { Push-Location $worktree; try { if ((git rev-parse HEAD).Trim() -ne $target) { throw 'WRONG_WORKTREE_HEAD' }; @('OPENROUTER_API_KEY','GITHUB_TOKEN','GH_TOKEN','DCOIR_GITHUB_FG_TOKEN','DCOIR_GITHUB_CL_TOKEN','DCOIR_GEMINI_API','DCOIR_OPENAI_API_KEY','DCOIR_OPENAI_PROJECT_ID','OPENAI_API_KEY') | ForEach-Object { Remove-Item (\"Env:\" + $_) -ErrorAction SilentlyContinue }; $env:PYTHONDONTWRITEBYTECODE = '1'; $selftest = '.github/dcoir_review/scripts/dcoir_review_runtime_module_loader_selftest.py'; python -c \"from pathlib import Path; p=Path(r'$selftest'); compile(p.read_text(encoding='utf-8'), str(p), 'exec')\"; if ($LASTEXITCODE -ne 0) { throw 'PY_COMPILE_FAILED' }; python $selftest; if ($LASTEXITCODE -ne 0) { throw 'MODULE_LOADER_SELFTEST_FAILED' }; $probe = '.github/dcoir_review/scripts/dcoir_review/selftests/__ownership_probe_unowned.py'; Set-Content -LiteralPath $probe -Value '# synthetic unowned module for fail-closed validation' -Encoding ASCII; try { cmd.exe /d /c \"python $selftest >NUL 2>&1\"; $probeExit = $LASTEXITCODE; if ($probeExit -eq 0) { throw 'UNOWNED_MODULE_WAS_NOT_REJECTED' } } finally { Remove-Item -LiteralPath $probe -Force -ErrorAction SilentlyContinue }; python $selftest; if ($LASTEXITCODE -ne 0) { throw 'SELFTEST_DID_NOT_RECOVER_AFTER_OWNERSHIP_PROBE' }; $provider = '.github/dcoir_review/scripts/dcoir_review/hardened/part_04a_provider.py'; Add-Content -LiteralPath $provider -Value '# synthetic size-growth probe' -Encoding ASCII; try { cmd.exe /d /c \"python $selftest >NUL 2>&1\"; $growthExit = $LASTEXITCODE; if ($growthExit -eq 0) { throw 'LEGACY_OVERSIZE_GROWTH_WAS_NOT_REJECTED' } } finally { git checkout -- $provider; if ($LASTEXITCODE -ne 0) { throw 'PROVIDER_RESTORE_FAILED' } }; python $selftest; if ($LASTEXITCODE -ne 0) { throw 'SELFTEST_DID_NOT_RECOVER_AFTER_SIZE_PROBE' }; git diff --check \"$base...$target\"; if ($LASTEXITCODE -ne 0) { throw 'DIFF_CHECK_FAILED' }; $dirty = @(git status --porcelain); if ($dirty.Count -ne 0) { throw ('WORKTREE_DIRTY_AFTER_VALIDATION:' + ($dirty -join ',')) } } finally { Pop-Location } } finally { git worktree remove --force $worktree }; Write-Output 'ISSUE550_PR552_MODULE_OWNERSHIP_VALIDATION_PASS'"
}
