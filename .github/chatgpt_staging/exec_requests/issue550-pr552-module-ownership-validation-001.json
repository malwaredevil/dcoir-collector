{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "issue550-pr552-module-ownership-validation-001",
  "operator_approved": true,
  "shell": "powershell_5",
  "timeout_seconds": 1800,
  "artifact_retention_days": 7,
  "cleanup_request_after_run": true,
  "approved_command_preview": "Exact-head validation for PR #552 module ownership repair. Verify the PR branch still points to cf4a22a51aa61f9873d02503cff5f3c19179fc51, verify baseline 0273ac05445a469849f81588377c106e6e62a242 remains an ancestor, use a detached clean worktree, remove provider/GitHub credentials, compile and run the module-loader selftest, prove the new ownership contract accepts the real provider_transport direct-import helpers, prove it still fails closed on a synthetic unowned Python module, run PR-range git diff --check, and require a clean worktree after cleanup. Do not invoke DCOIR Review, make provider calls, mutate PR #552, change Draft/Ready state, or merge.",
  "command": "$ErrorActionPreference = 'Stop'; $target = 'cf4a22a51aa61f9873d02503cff5f3c19179fc51'; $base = '0273ac05445a469849f81588377c106e6e62a242'; $branch = 'fix/issue-550-module-ownership-validation'; git fetch --no-tags origin \"refs/heads/$branch:refs/remotes/origin/$branch\" \"refs/heads/main:refs/remotes/origin/main\"; if ($LASTEXITCODE -ne 0) { throw 'FETCH_FAILED' }; $remoteHead = (git rev-parse \"refs/remotes/origin/$branch\").Trim(); if ($remoteHead -ne $target) { throw \"STALE_PR_HEAD:$remoteHead\" }; git merge-base --is-ancestor $base $target; if ($LASTEXITCODE -ne 0) { throw 'BASE_NOT_ANCESTOR_OF_TARGET' }; $worktree = Join-Path $env:RUNNER_TEMP 'issue550-pr552-exact-head'; if (Test-Path -LiteralPath $worktree) { Remove-Item -LiteralPath $worktree -Recurse -Force }; git worktree add --detach $worktree $target; if ($LASTEXITCODE -ne 0) { throw 'WORKTREE_ADD_FAILED' }; try { Push-Location $worktree; try { if ((git rev-parse HEAD).Trim() -ne $target) { throw 'WRONG_WORKTREE_HEAD' }; Remove-Item Env:OPENROUTER_API_KEY -ErrorAction SilentlyContinue; Remove-Item Env:GITHUB_TOKEN -ErrorAction SilentlyContinue; Remove-Item Env:GH_TOKEN -ErrorAction SilentlyContinue; python -m py_compile .github/dcoir_review/scripts/dcoir_review_runtime_module_loader_selftest.py; if ($LASTEXITCODE -ne 0) { throw 'PY_COMPILE_FAILED' }; python .github/dcoir_review/scripts/dcoir_review_runtime_module_loader_selftest.py; if ($LASTEXITCODE -ne 0) { throw 'MODULE_LOADER_SELFTEST_FAILED' }; $probe = '.github/dcoir_review/scripts/dcoir_review/selftests/__ownership_probe_unowned.py'; Set-Content -LiteralPath $probe -Value '# synthetic unowned module for fail-closed validation' -Encoding ASCII; try { python .github/dcoir_review/scripts/dcoir_review_runtime_module_loader_selftest.py *> $null; if ($LASTEXITCODE -eq 0) { throw 'UNOWNED_MODULE_WAS_NOT_REJECTED' } } finally { Remove-Item -LiteralPath $probe -Force -ErrorAction SilentlyContinue }; python .github/dcoir_review/scripts/dcoir_review_runtime_module_loader_selftest.py; if ($LASTEXITCODE -ne 0) { throw 'SELFTEST_DID_NOT_RECOVER_AFTER_PROBE' }; git diff --check \"$base...$target\"; if ($LASTEXITCODE -ne 0) { throw 'DIFF_CHECK_FAILED' }; $dirty = @(git status --porcelain); if ($dirty.Count -ne 0) { throw ('WORKTREE_DIRTY_AFTER_VALIDATION:' + ($dirty -join ',')) } } finally { Pop-Location } } finally { git worktree remove --force $worktree }; Write-Output 'ISSUE550_PR552_MODULE_OWNERSHIP_VALIDATION_PASS'"
}
