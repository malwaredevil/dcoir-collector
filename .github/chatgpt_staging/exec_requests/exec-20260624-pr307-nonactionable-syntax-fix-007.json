{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "exec-20260624-pr307-nonactionable-syntax-fix-007",
  "operator_approved": true,
  "approved_at_utc": "2026-06-24T16:36:00Z",
  "shell": "pwsh",
  "timeout_seconds": 900,
  "artifact_retention_days": 3,
  "cleanup_request_after_run": true,
  "approved_command_preview": "Fix the malformed newline string in .github/dcoir_review/scripts/openrouter_pr_review_hardened.py on PR #307, validate py_compile and hardened selftest before committing, then push the corrected branch.",
  "command": "$ErrorActionPreference = 'Stop'\n$branch = 'codex/issue-306-function-reachability-report'\n$runnerPath = '.github/dcoir_review/scripts/openrouter_pr_review_hardened.py'\n$selftestPath = '.github/dcoir_review/scripts/openrouter_pr_review_hardened_selftest.py'\n\nfunction Invoke-Git([string[]]$GitArgs) {\n  Write-Host \"git $($GitArgs -join ' ')\"\n  $output = & git @GitArgs\n  $code = $LASTEXITCODE\n  if ($output) { $output | Out-Host }\n  if ($code -ne 0) { throw \"git failed with exit $code`: git $($GitArgs -join ' ')\" }\n}\n\ntry {\n  Invoke-Git @('fetch', '--no-tags', 'origin', \"refs/heads/$branch`:refs/remotes/origin/$branch\")\n  Invoke-Git @('checkout', '-B', $branch, \"origin/$branch\")\n  Invoke-Git @('config', 'user.name', 'github-actions[bot]')\n  Invoke-Git @('config', 'user.email', '41898282+github-actions[bot]@users.noreply.github.com')\n\n  $fixScript = @'\nfrom pathlib import Path\n\npath = Path(\".github/dcoir_review/scripts/openrouter_pr_review_hardened.py\")\ntext = path.read_text(encoding=\"utf-8\")\nbad = '    return re.sub(r\"\\\\s+\", \" \", \"\\n\".join(parts)).strip()'\ngood = '    return re.sub(r\"\\\\s+\", \" \", \"\\\\n\".join(parts)).strip()'\nif bad not in text:\n    raise SystemExit(\"expected malformed finding_text_for_quality line not found\")\ntext = text.replace(bad, good, 1)\npath.write_text(text, encoding=\"utf-8\")\nprint(\"fixed malformed newline join in finding_text_for_quality\")\n'@\n  $scriptPath = Join-Path $env:RUNNER_TEMP 'fix_dcoir_review_nonactionable_syntax.py'\n  $fixScript | Set-Content -LiteralPath $scriptPath -Encoding UTF8\n  python $scriptPath\n  if ($LASTEXITCODE -ne 0) { throw 'syntax fix script failed' }\n\n  python -m py_compile $runnerPath $selftestPath\n  if ($LASTEXITCODE -ne 0) { throw 'py_compile failed after syntax fix' }\n  python $selftestPath\n  if ($LASTEXITCODE -ne 0) { throw 'hardened selftest failed after syntax fix' }\n  Invoke-Git @('diff', '--check', '--', $runnerPath, $selftestPath)\n\n  & git diff --quiet -- $runnerPath\n  $diffCode = $LASTEXITCODE\n  if ($diffCode -eq 0) { throw 'syntax fix produced no runner diff' }\n  if ($diffCode -ne 1) { throw \"git diff failed for $runnerPath with exit $diffCode\" }\n  Invoke-Git @('add', '--', $runnerPath)\n  Invoke-Git @('commit', '-m', 'Fix dcoir review non-actionable syntax')\n  Invoke-Git @('push', 'origin', \"HEAD:refs/heads/$branch\")\n  Write-Host 'Pushed branch head:'\n  Invoke-Git @('rev-parse', 'HEAD')\n}\nfinally {\n  try {\n    Invoke-Git @('fetch', '--no-tags', 'origin', 'main')\n    Invoke-Git @('checkout', '-B', 'main', 'origin/main')\n    Invoke-Git @('status', '--short')\n  } catch {\n    Write-Warning \"Unable to restore workflow workspace to main: $_\"\n  }\n}\n"
}
