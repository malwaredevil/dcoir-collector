{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "exec-20260902-pr465-v41-exact-head-validation-005",
  "operator_approved": true,
  "command": "$ErrorActionPreference = 'Stop'; $sha = 'ed1141e1a8c86da65d3987202c03cf51a8a24049'; $worktree = Join-Path $env:RUNNER_TEMP 'dcoir-pr465-v41-validation'; $results = @(); $failure = ''; $actual = ''; if (Test-Path -LiteralPath $worktree) { & git worktree remove --force $worktree 2>$null | Out-Null }; & git fetch --no-tags origin $sha; if ($LASTEXITCODE -ne 0) { throw 'git fetch of approved PR head failed' }; & git worktree add --detach $worktree $sha; if ($LASTEXITCODE -ne 0) { throw 'git worktree add for approved PR head failed' }; Push-Location $worktree; try { $actual = (& git rev-parse HEAD).Trim(); if ($actual -ne $sha) { throw \"Exact-head mismatch: expected $sha got $actual\" }; Write-Host \"Validated detached worktree head: $actual\"; & python .github/dcoir_review/scripts/dcoir_review_required_runtime_patch_v41_selftest.py; $code = $LASTEXITCODE; $results += [pscustomobject]@{ phase = 'v41-selftest-first'; command = 'python .github/dcoir_review/scripts/dcoir_review_required_runtime_patch_v41_selftest.py'; exit_code = $code }; if ($code -ne 0) { throw \"v41 selftest failed with exit code $code\" }; $commandsJson = & python -c \"import sys,json; sys.path.insert(0,r'.github/dcoir_review/scripts'); import openrouter_pr_review_pareto_context as r; print(json.dumps(r.load_pareto_context_config(r'.github/dcoir_review/openrouter-pr-review-pareto.yml').validation_commands))\"; if ($LASTEXITCODE -ne 0) { throw 'failed to enumerate governed validation commands' }; $commands = @($commandsJson | ConvertFrom-Json); Write-Host \"Governed validation command count: $($commands.Count)\"; $index = 0; foreach ($validationCommand in $commands) { $index += 1; Write-Host \"=== governed validation $index/$($commands.Count): $validationCommand\"; & cmd.exe /d /s /c $validationCommand; $code = $LASTEXITCODE; $results += [pscustomobject]@{ phase = 'governed-validation'; index = $index; command = [string]$validationCommand; exit_code = $code }; if ($code -ne 0) { throw \"Governed validation command $index failed with exit code ${code}: $validationCommand\" } }; Write-Host 'All exact-head governed DCOIR validation commands passed.' } catch { $failure = $_.Exception.Message; throw } finally { try { if (-not [string]::IsNullOrWhiteSpace($env:DCOIR_DOWNLOADS_DIR)) { [ordered]@{ schema = 'dcoir.pr465.v41.validation.v1'; requested_sha = $sha; actual_sha = $actual; result = if ([string]::IsNullOrWhiteSpace($failure)) { 'success' } else { 'failure' }; failure = $failure; results = $results } | ConvertTo-Json -Depth 8 | Out-File -LiteralPath (Join-Path $env:DCOIR_DOWNLOADS_DIR 'pr465-v41-exact-head-validation-summary.json') -Encoding utf8 } } catch { Write-Warning \"Could not write validation summary: $($_.Exception.Message)\" }; Pop-Location; & git worktree remove --force $worktree 2>$null | Out-Null }",
  "approved_command_preview": "Validate PR #465 exact head ed1141e1a8c86da65d3987202c03cf51a8a24049 in a detached worktree: run the trusted-provenance v41 selftest first, then every configured DCOIR validation command, write a structured summary artifact, and do not modify the PR branch or call OpenRouter models.",
  "shell": "pwsh",
  "timeout_seconds": 1800,
  "artifact_retention_days": 7,
  "cleanup_request_after_run": true,
  "trigger_nonce": "20260902T1312Z"
}
