{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "issue550-pr552-post-copilot-validation-005",
  "operator_approved": true,
  "shell": "powershell_5",
  "timeout_seconds": 1800,
  "artifact_retention_days": 7,
  "cleanup_request_after_run": true,
  "approved_command_preview": "Fresh exact-head validation for PR #552 after Copilot fixed the all-LAYER_SEGMENTS coverage gap at 0c9a94f7648028e45c814947eead0387f52653ea. Verify original-base ancestry and current main ancestry, exact remote PR head, clean detached worktree, secret removal, syntax compilation, clean selftest, fail-closed unowned-module rejection, frozen legacy-provider growth rejection, and a new regression proving an oversized segment in non-adjacency layer dcoir_review_required_runtime_patch_v18 is rejected. Restore after each negative probe, rerun clean selftest, assert the PR changes exactly one intended selftest file, run diff --check, and finish clean. No DCOIR Review, provider calls, Ready transition, or merge.",
  "command": "$ErrorActionPreference = 'Stop'; $target = '0c9a94f7648028e45c814947eead0387f52653ea'; $base = '0273ac05445a469849f81588377c106e6e62a242'; $branch = 'fix/issue-550-module-ownership-validation'; git fetch --no-tags origin \"refs/heads/${branch}:refs/remotes/origin/${branch}\" \"refs/heads/main:refs/remotes/origin/main\"; if ($LASTEXITCODE -ne 0) { throw 'FETCH_FAILED' }; $remoteHead = (git rev-parse \"refs/remotes/origin/${branch}\").Trim(); if ($remoteHead -ne $target) { throw \"STALE_PR_HEAD:$remoteHead\" }; $currentMain = (git rev-parse 'refs/remotes/origin/main').Trim(); git merge-base --is-ancestor $base $target; if ($LASTEXITCODE -ne 0) { throw 'BASE_NOT_ANCESTOR_OF_TARGET' }; git merge-base --is-ancestor $base $currentMain; if ($LASTEXITCODE -ne 0) { throw 'BASE_NOT_ANCESTOR_OF_CURRENT_MAIN' }; $worktree = Join-Path $env:RUNNER_TEMP 'issue550-pr552-exact-head-005'; if (Test-Path -LiteralPath $worktree) { Remove-Item -LiteralPath $worktree -Recurse -Force }; git worktree add --detach $worktree $target; if ($LASTEXITCODE -ne 0) { throw 'WORKTREE_ADD_FAILED' }; try { Push-Location $worktree; try { if ((git rev-parse HEAD).Trim() -ne $target) { throw 'WRONG_WORKTREE_HEAD' }; @('OPENROUTER_API_KEY','GITHUB_TOKEN','GH_TOKEN','DCOIR_GITHUB_FG_TOKEN','DCOIR_GITHUB_CL_TOKEN','DCOIR_GEMINI_API','DCOIR_OPENAI_API_KEY','DCOIR_OPENAI_PROJECT_ID','OPENAI_API_KEY') | ForEach-Object { Remove-Item (\"Env:\" + $_) -ErrorAction SilentlyContinue }; $env:PYTHONDONTWRITEBYTECODE = '1'; $selftest = '.github/dcoir_review/scripts/dcoir_review_runtime_module_loader_selftest.py'; python -c \"from pathlib import Path; p=Path(r'$selftest'); compile(p.read_text(encoding='utf-8'), str(p), 'exec')\"; if ($LASTEXITCODE -ne 0) { throw 'PY_COMPILE_FAILED' }; python $selftest; if ($LASTEXITCODE -ne 0) { throw 'MODULE_LOADER_SELFTEST_FAILED' }; $changed = @(git diff --name-only \"$base...$target\"); if ($changed.Count -ne 1 -or $changed[0] -ne $selftest) { throw ('UNEXPECTED_PR_SCOPE:' + ($changed -join ',')) }; $probe = '.github/dcoir_review/scripts/dcoir_review/selftests/__ownership_probe_unowned.py'; Set-Content -LiteralPath $probe -Value '# synthetic unowned module for fail-closed validation' -Encoding ASCII; try { cmd.exe /d /c \"python $selftest >NUL 2>&1\"; $probeExit = $LASTEXITCODE; if ($probeExit -eq 0) { throw 'UNOWNED_MODULE_WAS_NOT_REJECTED' } } finally { Remove-Item -LiteralPath $probe -Force -ErrorAction SilentlyContinue }; python $selftest; if ($LASTEXITCODE -ne 0) { throw 'SELFTEST_DID_NOT_RECOVER_AFTER_OWNERSHIP_PROBE' }; $provider = '.github/dcoir_review/scripts/dcoir_review/hardened/part_04a_provider.py'; Add-Content -LiteralPath $provider -Value '# synthetic size-growth probe' -Encoding ASCII; try { cmd.exe /d /c \"python $selftest >NUL 2>&1\"; $providerExit = $LASTEXITCODE; if ($providerExit -eq 0) { throw 'LEGACY_OVERSIZE_GROWTH_WAS_NOT_REJECTED' } } finally { git checkout -- $provider; if ($LASTEXITCODE -ne 0) { throw 'PROVIDER_RESTORE_FAILED' } }; python $selftest; if ($LASTEXITCODE -ne 0) { throw 'SELFTEST_DID_NOT_RECOVER_AFTER_PROVIDER_PROBE' }; $nonAdjacent = '.github/dcoir_review/scripts/dcoir_review/patches/dcoir_review_required_runtime_patch_v18/part_01.py'; $oversizeLine = '#' + ('x' * 16000); Add-Content -LiteralPath $nonAdjacent -Value $oversizeLine -Encoding ASCII; try { cmd.exe /d /c \"python $selftest >NUL 2>&1\"; $nonAdjacentExit = $LASTEXITCODE; if ($nonAdjacentExit -eq 0) { throw 'NON_ADJACENCY_LAYER_OVERSIZE_WAS_NOT_REJECTED' } } finally { git checkout -- $nonAdjacent; if ($LASTEXITCODE -ne 0) { throw 'NON_ADJACENCY_RESTORE_FAILED' } }; python $selftest; if ($LASTEXITCODE -ne 0) { throw 'SELFTEST_DID_NOT_RECOVER_AFTER_NON_ADJACENCY_PROBE' }; git diff --check \"$base...$target\"; if ($LASTEXITCODE -ne 0) { throw 'DIFF_CHECK_FAILED' }; $dirty = @(git status --porcelain); if ($dirty.Count -ne 0) { throw ('WORKTREE_DIRTY_AFTER_VALIDATION:' + ($dirty -join ',')) } } finally { Pop-Location } } finally { git worktree remove --force $worktree }; Write-Output ('ISSUE550_PR552_POST_COPILOT_VALIDATION_PASS target=' + $target + ' current_main=' + $currentMain)"
}
