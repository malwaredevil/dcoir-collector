{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "issue484-pr501-adva-fix-and-validate-v1-20260908T0625Z",
  "operator_approved": true,
  "approved_at_utc": "2026-09-08T06:25:00Z",
  "shell": "powershell_5",
  "timeout_seconds": 1800,
  "artifact_retention_days": 7,
  "cleanup_request_after_run": true,
  "approved_command_preview": "$ErrorActionPreference='Stop'; guard PR #501 branch at e8b271f2; apply only the staged issue #484 adversarial source/test fix to the two existing PR files; remove inference credentials; run Python compile, focused lane-scoring regressions, and the full deterministic Gemini replay suite; verify the diff is limited to those two files; commit and push by exact force-with-lease; do not invoke a model review, mark Ready, or merge.",
  "command": "$ErrorActionPreference='Stop'; $repo=$env:DCOIR_REPO_ROOT; if (-not $repo) { throw 'DCOIR_REPO_ROOT is not set' }; $branch='issue-484-lane-separation-scoring'; $expected='e8b271f2fda20dbf3cad9cd22b4fe354e7249d66'; $remoteLine=git -C $repo ls-remote origin ('refs/heads/' + $branch); if ($LASTEXITCODE -ne 0) { throw 'git ls-remote failed' }; $remote=(($remoteLine -split '\\s+')[0]); if ($remote -ne $expected) { throw ('PR head drift: expected ' + $expected + ', got ' + $remote) }; $worktree=Join-Path $env:RUNNER_TEMP 'dcoir-issue484-pr501-adva-fix'; if (Test-Path $worktree) { git -C $repo worktree remove --force $worktree 2>$null; Remove-Item -LiteralPath $worktree -Recurse -Force -ErrorAction SilentlyContinue }; git -C $repo fetch --no-tags origin $branch; if ($LASTEXITCODE -ne 0) { throw 'git fetch failed' }; git -C $repo worktree add --detach $worktree $expected; if ($LASTEXITCODE -ne 0) { throw 'git worktree add failed' }; try { 'OPENROUTER_API_KEY','OPENAI_API_KEY','GEMINI_API_KEY','GOOGLE_API_KEY','DCOIR_GEMINI_API','DCOIR_OPENAI_API_KEY','DCOIR_OPENAI_PROJECT_ID' | ForEach-Object { Remove-Item ('Env:' + $_) -ErrorAction SilentlyContinue }; $env:PYTHONDONTWRITEBYTECODE='1'; $patchScript=Join-Path $repo '.github/chatgpt_staging/exec_scripts/issue484-pr501-adva-fix-v1-20260908.py'; python $patchScript --worktree $worktree; if ($LASTEXITCODE -ne 0) { throw 'source patch script failed' }; Set-Location $worktree; python -m py_compile project_sources/gemini/tools/lib/gemini_behavioral_replay_scoring.py project_sources/gemini/tools/validate_gemini_lane_separation_scoring.py; if ($LASTEXITCODE -ne 0) { throw 'py_compile failed' }; python project_sources/gemini/tools/validate_gemini_lane_separation_scoring.py; if ($LASTEXITCODE -ne 0) { throw 'focused lane-separation regression failed' }; $out=Join-Path $env:RUNNER_TEMP 'issue484-gemini-replay-adva-fix-output'; if (Test-Path $out) { Remove-Item -LiteralPath $out -Recurse -Force }; python project_sources/gemini/tools/run_gemini_behavioral_replay_validation_suite.py --fixtures-root project_sources/gemini/fixtures/behavioral_replay --output-dir $out; if ($LASTEXITCODE -ne 0) { throw 'Gemini behavioral replay validation suite failed' }; $changed=@(git diff --name-only); $expectedFiles=@('project_sources/gemini/tools/lib/gemini_behavioral_replay_scoring.py','project_sources/gemini/tools/validate_gemini_lane_separation_scoring.py'); if ((Compare-Object -ReferenceObject ($expectedFiles | Sort-Object) -DifferenceObject ($changed | Sort-Object))) { throw ('unexpected changed-file set: ' + ($changed -join ', ')) }; if (git diff --name-only | Select-String -SimpleMatch '.github/workflows/') { throw 'workflow YAML changed unexpectedly' }; git config user.name 'Malware Devil'; git config user.email '34285973+malwaredevil@users.noreply.github.com'; git add -- $expectedFiles; git commit -m 'Harden lane relationship scoring regressions'; if ($LASTEXITCODE -ne 0) { throw 'git commit failed' }; $newHead=(git rev-parse HEAD).Trim(); $remoteBefore=(git ls-remote origin ('refs/heads/' + $branch) | ForEach-Object { ($_ -split '\\s+')[0] }); if ($remoteBefore -ne $expected) { throw ('PR head drift before push: expected ' + $expected + ', got ' + $remoteBefore) }; git push origin ('HEAD:refs/heads/' + $branch) ('--force-with-lease=refs/heads/' + $branch + ':' + $expected); if ($LASTEXITCODE -ne 0) { throw 'git push failed' }; $remoteAfter=(git ls-remote origin ('refs/heads/' + $branch) | ForEach-Object { ($_ -split '\\s+')[0] }); if ($remoteAfter -ne $newHead) { throw ('push readback mismatch: expected ' + $newHead + ', got ' + $remoteAfter) }; if (git status --porcelain) { throw 'post-commit worktree is not clean' }; Write-Host ('Issue #484 adversarial fix pushed and validated at ' + $newHead) } finally { Set-Location $repo; git worktree remove --force $worktree 2>$null }"
}
