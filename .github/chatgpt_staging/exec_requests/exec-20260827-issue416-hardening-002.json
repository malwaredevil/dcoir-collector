{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "exec-20260827-issue416-hardening-002",
  "operator_approved": true,
  "approved_at_utc": "2026-08-27T06:08:00Z",
  "shell": "pwsh",
  "timeout_seconds": 1800,
  "artifact_retention_days": 3,
  "cleanup_request_after_run": true,
  "approved_command_preview": "Harden issue #416 evaluator on exact branch head 49dd38632124a05e374b50d897579cf13102eb19: validate safe candidate group ids, fail closed on evidence/hash readback failures, expose Gemini bundle identity, require provenance for pass/fail live evidence, add regressions, and wire evaluator tests into the existing Knowledge projection self-test command. Validate, commit, and push only the issue branch, then return runner to main.",
  "command": "$ErrorActionPreference = 'Stop'\n$targetSha = '49dd38632124a05e374b50d897579cf13102eb19'\n$branch = 'issue-416-gemini-knowledge-consolidation-eval'\ngit fetch --no-tags origin $branch main\nif ($LASTEXITCODE -ne 0) { throw 'Unable to fetch issue branch/main' }\ngit checkout -B $branch $targetSha\nif ($LASTEXITCODE -ne 0) { throw 'Unable to checkout issue branch base' }\nif ((git rev-parse HEAD).Trim() -ne $targetSha) { throw 'Issue branch stale-base check failed' }\n@'\nfrom pathlib import Path\n\ntool = Path('project_sources/agent_runtime/tools/evaluate_gemini_knowledge_consolidation.py')\ntext = tool.read_text(encoding='utf-8')\n\ndef replace_once(old, new):\n    global text\n    if text.count(old) != 1:\n        raise SystemExit(f'expected exactly one tool match for: {old[:80]!r}; got {text.count(old)}')\n    text = text.replace(old, new, 1)\n\nreplace_once('import argparse\\nimport json\\nimport sys\\n', 'import argparse\\nimport json\\nimport re\\nimport sys\\n')\nreplace_once(\n    \"VALID_LIVE_EVIDENCE = ('unavailable', 'pass', 'fail')\\n\\n\\ndef _json_bytes(value: Any) -> bytes:\\n    return (json.dumps(value, indent=2, sort_keys=True) + '\\\\n').encode('utf-8')\\n\\n\\ndef _sha256(data: bytes) -> str:\\n    return knowledge_projection._sha256(data)\\n\",\n    \"VALID_LIVE_EVIDENCE = ('unavailable', 'pass', 'fail')\\nSAFE_GROUP_ID_RE = re.compile(r'^[a-z0-9][a-z0-9_]*$')\\n\\n\\ndef _sha256(data: bytes) -> str:\\n    return knowledge_projection._sha256(data)\\n\\n\\ndef _file_sha256(\\n    path: Path, label: str, errors: list[str]\\n) -> str | None:\\n    try:\\n        return _sha256(path.read_bytes())\\n    except (OSError, RuntimeError) as exc:\\n        errors.append(f'Unable to hash {label} {path}: {exc}')\\n        return None\\n\"\n)\nreplace_once(\n    \"        if not isinstance(group_id, str) or not group_id:\\n            errors.append('Reference projection group lacks an id')\\n            continue\\n        if group_id in seen:\\n\",\n    \"        if not isinstance(group_id, str) or not group_id:\\n            errors.append('Reference projection group lacks an id')\\n            continue\\n        if not SAFE_GROUP_ID_RE.fullmatch(group_id):\\n            errors.append(f'Unsafe reference projection group id: {group_id}')\\n            continue\\n        if group_id in seen:\\n\"\n)\nreplace_once(\n    \"    if live_evidence_status == 'pass' and not live_evidence_run:\\n        errors.append(\\n            'live_evidence_run is required when live_evidence_status is pass'\\n        )\\n\",\n    \"    if live_evidence_status in {'pass', 'fail'} and not live_evidence_run:\\n        errors.append(\\n            'live_evidence_run is required when live_evidence_status is pass or fail'\\n        )\\n\"\n)\nreplace_once(\n    \"        elif not isinstance(group_id, str) or not group_id:\\n            errors.append(\\n                f\\\"{record['id']} has invalid openai_dcoir_projection_group\\\"\\n            )\\n            continue\\n        group_sources[group_id].append(record)\\n\",\n    \"        elif not isinstance(group_id, str) or not group_id:\\n            errors.append(\\n                f\\\"{record['id']} has invalid openai_dcoir_projection_group\\\"\\n            )\\n            continue\\n        elif not SAFE_GROUP_ID_RE.fullmatch(group_id):\\n            errors.append(\\n                f\\\"{record['id']} has unsafe openai_dcoir_projection_group: {group_id}\\\"\\n            )\\n            continue\\n        group_sources[group_id].append(record)\\n\"\n)\nreplace_once(\n    \"    gemini_records = [\\n\",\n    \"    projection_manifest_sha256 = _file_sha256(\\n        resolved_manifest, 'projection manifest', errors\\n    )\\n    source_contract_sha256 = (\\n        _file_sha256(source_contract_path, 'source contract', errors)\\n        if source_contract_path is not None\\n        else None\\n    )\\n    bundle_manifest_sha256 = (\\n        _file_sha256(bundle_path, 'Gemini bundle manifest', errors)\\n        if bundle_path is not None\\n        else None\\n    )\\n\\n    gemini_records = [\\n\"\n)\nreplace_once(\n    \"            'projection_manifest_sha256': _sha256(resolved_manifest.read_bytes()),\\n\",\n    \"            'projection_manifest_sha256': projection_manifest_sha256,\\n\"\n)\nreplace_once(\n    \"            'source_contract_sha256': (\\n                _sha256(source_contract_path.read_bytes())\\n                if source_contract_path is not None and source_contract_path.exists()\\n                else None\\n            ),\\n\",\n    \"            'source_contract_sha256': source_contract_sha256,\\n\"\n)\nreplace_once(\n    \"            'gemini_bundle_manifest_sha256': (\\n                _sha256(bundle_path.read_bytes())\\n                if bundle_path is not None and bundle_path.exists()\\n                else None\\n            ),\\n            'active_mode': active_mode,\\n\",\n    \"            'gemini_bundle_manifest_sha256': bundle_manifest_sha256,\\n            'gemini_bundle_version': (\\n                bundle.get('bundle_version') if isinstance(bundle, dict) else None\\n            ),\\n            'gemini_bundle_source_strategy': (\\n                bundle.get('source_strategy') if isinstance(bundle, dict) else None\\n            ),\\n            'active_mode': active_mode,\\n\"\n)\ntool.write_text(text, encoding='utf-8')\n\ntest = Path('project_sources/agent_runtime/tests/evaluate_gemini_knowledge_consolidation_selftest.py')\nt = test.read_text(encoding='utf-8')\nneedle = \"    def test_live_failure_defers(self) -> None:\\n        errors, report = self._evaluate(\\n            live_status='fail',\\n            live_run='https://example.invalid/run/124',\\n        )\\n        self.assertEqual(errors, [])\\n        self.assertEqual(report['decision']['recommended'], 'DEFER')\\n\\n\"\naddition = needle + \"    def test_live_failure_requires_specific_run(self) -> None:\\n        errors, report = self._evaluate(live_status='fail')\\n        self.assertTrue(\\n            any('live_evidence_run is required' in error for error in errors)\\n        )\\n        self.assertEqual(report['decision']['recommended'], 'REVISE')\\n\\n\"\nif t.count(needle) != 1:\n    raise SystemExit('live failure test insertion anchor mismatch')\nt = t.replace(needle, addition, 1)\nneedle2 = \"    def test_active_gemini_mode_cannot_be_silently_changed(self) -> None:\\n\"\nunsafe_test = \"    def test_unsafe_candidate_group_id_is_rejected(self) -> None:\\n        contract = self._read_json(self.contract_path)\\n        contract['knowledge_projection_groups'][0]['id'] = 'bad/group'\\n        contract['knowledge_items'][0]['openai_dcoir_projection_group'] = 'bad/group'\\n        contract['knowledge_items'][3]['openai_dcoir_projection_group'] = 'bad/group'\\n        self._write_contract(contract)\\n        errors, report = self._evaluate()\\n        self.assertTrue(\\n            any('unsafe' in error.casefold() and 'group' in error.casefold() for error in errors),\\n            errors,\\n        )\\n        self.assertEqual(report['decision']['recommended'], 'REVISE')\\n\\n\" + needle2\nif t.count(needle2) != 1:\n    raise SystemExit('unsafe group test insertion anchor mismatch')\nt = t.replace(needle2, unsafe_test, 1)\ntest.write_text(t, encoding='utf-8')\n\nintegration = Path('project_sources/agent_runtime/tests/project_agent_knowledge_selftest.py')\ni = integration.read_text(encoding='utf-8')\nanchor = 'import project_agent_knowledge as projector  # noqa: E402\\n'\ninsert = anchor + \"from evaluate_gemini_knowledge_consolidation_selftest import (  # noqa: E402,F401\\n    GeminiKnowledgeConsolidationEvaluationSelfTest,\\n)\\n\"\nif i.count(anchor) != 1:\n    raise SystemExit('knowledge self-test integration anchor mismatch')\ni = i.replace(anchor, insert, 1)\nintegration.write_text(i, encoding='utf-8')\n'@ | python -\nif ($LASTEXITCODE -ne 0) { throw 'Source hardening edit failed' }\npython -m py_compile project_sources/agent_runtime/tools/evaluate_gemini_knowledge_consolidation.py project_sources/agent_runtime/tests/evaluate_gemini_knowledge_consolidation_selftest.py project_sources/agent_runtime/tests/project_agent_knowledge_selftest.py\nif ($LASTEXITCODE -ne 0) { throw 'Compile failed' }\npython project_sources/agent_runtime/tests/evaluate_gemini_knowledge_consolidation_selftest.py\nif ($LASTEXITCODE -ne 0) { throw 'Evaluator self-tests failed' }\npython project_sources/agent_runtime/tests/project_agent_knowledge_selftest.py\nif ($LASTEXITCODE -ne 0) { throw 'Integrated Knowledge self-tests failed' }\npython project_sources/agent_runtime/tools/project_agent_knowledge.py --check *> $null\nif ($LASTEXITCODE -ne 0) { throw 'Knowledge projection check failed' }\npython project_sources/agent_runtime/tools/evaluate_gemini_knowledge_consolidation.py --baseline-commit $targetSha *> $null\nif ($LASTEXITCODE -ne 0) { throw 'Real-repo evaluation failed' }\ngit diff --check\nif ($LASTEXITCODE -ne 0) { throw 'git diff --check failed' }\ngit add project_sources/agent_runtime/tools/evaluate_gemini_knowledge_consolidation.py project_sources/agent_runtime/tests/evaluate_gemini_knowledge_consolidation_selftest.py project_sources/agent_runtime/tests/project_agent_knowledge_selftest.py\ngit diff --cached --quiet\nif ($LASTEXITCODE -eq 0) { throw 'Hardening produced no staged changes' }\ngit -c user.name='Malware Devil' -c user.email='34285973+malwaredevil@users.noreply.github.com' commit -m 'Harden Gemini consolidation evaluation'\nif ($LASTEXITCODE -ne 0) { throw 'Commit failed' }\n$newSha = (git rev-parse HEAD).Trim()\ngit push origin HEAD:$branch\nif ($LASTEXITCODE -ne 0) { throw 'Push failed' }\nWrite-Host \"updated_branch_head=$newSha\"\ngit fetch --no-tags origin main\ngit checkout -B main origin/main\nif ($LASTEXITCODE -ne 0) { throw 'Unable to return runner to main' }\n"
}
