{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "cleanup-benchmark-478-refs-20260907T1745Z",
  "operator_approved": true,
  "approved_at_utc": "2026-09-07T17:45:00Z",
  "shell": "powershell_5",
  "timeout_seconds": 300,
  "artifact_retention_days": 3,
  "cleanup_request_after_run": true,
  "approved_command_preview": "Delete only the two completed #478 benchmark branches benchmark-478-architecture-b-replay-base and benchmark-478-architecture-b-replay-head, then verify both refs are absent. Preserve main and issue-497-preserve-candidate-identity. No source/PR mutation, review invocation, Ready transition, or merge.",
  "command": "$ErrorActionPreference = 'Continue'; $targets = @('benchmark-478-architecture-b-replay-base','benchmark-478-architecture-b-replay-head'); foreach ($b in $targets) { git ls-remote --exit-code --heads origin \"refs/heads/$b\" | Out-Null; $probe = $LASTEXITCODE; if ($probe -eq 0) { git push origin --delete $b; if ($LASTEXITCODE -ne 0) { throw \"Failed to delete branch $b\" } } elseif ($probe -ne 2) { throw \"Failed to query branch $b (exit $probe)\" } }; foreach ($b in $targets) { git ls-remote --exit-code --heads origin \"refs/heads/$b\" | Out-Null; $probe = $LASTEXITCODE; if ($probe -eq 0) { throw \"Branch still exists after deletion: $b\" } elseif ($probe -ne 2) { throw \"Failed to verify branch $b (exit $probe)\" } }; Write-Output 'Deleted and verified #478 benchmark refs.'; exit 0"
}
