{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "issue484-pr501-dcoir-medium-target-modifier-fix-v1-20260908T0846Z",
  "operator_approved": true,
  "approved_at_utc": "2026-09-08T08:46:00Z",
  "shell": "pwsh",
  "timeout_seconds": 1800,
  "artifact_retention_days": 7,
  "cleanup_request_after_run": true,
  "approved_command_preview": "On exact PR #501 head c5d715b2a32853fafbe7eefefcf893d7f0dbff6b, repair the verified DCOIR MEDIUM trailing-target determiner/possessive false-negative by stripping only a bounded closed set of leading target-head modifiers before endpoint/local head classification, add focused positive and contamination regressions, run diff-check, Python syntax, focused lane scoring, and the full deterministic Gemini replay suite, then commit and push only the two intended Python files.",
  "command": "$ErrorActionPreference = 'Stop'\n$branch = 'issue-484-lane-separation-scoring'\n$expectedHead = 'c5d715b2a32853fafbe7eefefcf893d7f0dbff6b'\n$work = Join-Path $env:RUNNER_TEMP 'issue484-target-modifier-fix-v1'\nif (Test-Path -LiteralPath $work) { Remove-Item -LiteralPath $work -Recurse -Force }\n& git fetch --no-tags origin \"refs/heads/$branch`:refs/remotes/origin/$branch\"\nif ($LASTEXITCODE -ne 0) { throw 'fetch failed' }\n$remoteHead = (& git rev-parse \"refs/remotes/origin/$branch\").Trim()\nWrite-Host \"REMOTE_HEAD=$remoteHead\"\nif ($remoteHead -ne $expectedHead) { throw \"head drift: expected $expectedHead got $remoteHead\" }\n& git config core.autocrlf false\n& git config core.eol lf\n& git worktree add --detach $work $expectedHead\nif ($LASTEXITCODE -ne 0) { throw 'worktree add failed' }\ntry {\n  Set-Location $work\n  $sourcePath = 'project_sources/gemini/tools/lib/gemini_behavioral_replay_scoring.py'\n  $validatorPath = 'project_sources/gemini/tools/validate_gemini_lane_separation_scoring.py'\n  $patchScript = @'\nfrom pathlib import Path\nsource_path = Path('project_sources/gemini/tools/lib/gemini_behavioral_replay_scoring.py')\nvalidator_path = Path('project_sources/gemini/tools/validate_gemini_lane_separation_scoring.py')\nsource = source_path.read_text(encoding='utf-8')\nvalidator = validator_path.read_text(encoding='utf-8')\nold_constants = '''_SHARED_CONTEXT_TERMS = (\n    "same shell",\n    "single shell",\n    "one shell",\n    "same command",\n    "single command",\n    "same lane",\n)\n'''\nnew_constants = old_constants + '''_LANE_TARGET_HEAD_MODIFIERS = frozenset(\n    {\n        "a",\n        "an",\n        "the",\n        "my",\n        "your",\n        "our",\n        "their",\n        "his",\n        "her",\n        "its",\n        "this",\n        "that",\n        "these",\n        "those",\n        "any",\n        "each",\n        "every",\n        "either",\n        "neither",\n        "both",\n        "all",\n        "some",\n        "one",\n    }\n)\n'''\nif source.count(old_constants) != 1:\n    raise SystemExit(f'constants anchor match count={source.count(old_constants)}')\nsource = source.replace(old_constants, new_constants)\nold_head = '''    target = normalize_text(relation.group("target"))\n    tokens = re.findall(r"[a-z0-9-]+", target)\n    target_starts_endpoint = bool(\n        tokens\n        and (\n            tokens[0] in {"endpoint", "response-action"}\n            or tokens[:2] == ["response", "action"]\n        )\n    )\n    target_starts_local = bool(\n        tokens and tokens[0] in {"local", "workstation"}\n    )\n'''\nnew_head = '''    target = normalize_text(relation.group("target"))\n    tokens = re.findall(r"[a-z0-9-]+", target)\n    target_head_tokens = list(tokens)\n    while (\n        target_head_tokens\n        and target_head_tokens[0] in _LANE_TARGET_HEAD_MODIFIERS\n    ):\n        target_head_tokens = target_head_tokens[1:]\n\n    target_starts_endpoint = bool(\n        target_head_tokens\n        and (\n            target_head_tokens[0] in {"endpoint", "response-action"}\n            or target_head_tokens[:2] == ["response", "action"]\n        )\n    )\n    target_starts_local = bool(\n        target_head_tokens\n        and target_head_tokens[0] in {"local", "workstation"}\n    )\n'''\nif source.count(old_head) != 1:\n    raise SystemExit(f'head anchor match count={source.count(old_head)}')\nsource = source.replace(old_head, new_head)\nanchor = '''    _expect(\n        "Do not run local workstation PowerShell in the same shell as endpoint "\n        "response-action commands.",\n        True,\n        "trailing-endpoint shared-shell negation",\n    )\n'''\ninsert = anchor + '''    _expect(\n        "Do not run endpoint response-action commands in the same shell as your local "\n        "PowerShell commands.",\n        True,\n        "possessive trailing-local shared-shell negation",\n    )\n    _expect(\n        "Do not run endpoint response-action commands in the same shell as my local "\n        "PowerShell commands.",\n        True,\n        "first-person possessive trailing-local shared-shell negation",\n    )\n    _expect(\n        "Do not run endpoint response-action commands in the same shell as her local "\n        "PowerShell commands.",\n        True,\n        "third-person possessive trailing-local shared-shell negation",\n    )\n    _expect(\n        "Do not run local workstation PowerShell in the same shell as an endpoint "\n        "response-action console.",\n        True,\n        "article trailing-endpoint shared-shell negation",\n    )\n    _expect(\n        "Do not run endpoint response-action commands in the same shell as my local "\n        "response-action tooling and PowerShell commands.",\n        False,\n        "possessive target cannot hide endpoint contamination",\n    )\n'''\nif validator.count(anchor) != 1:\n    raise SystemExit(f'validator anchor match count={validator.count(anchor)}')\nvalidator = validator.replace(anchor, insert)\nsource_path.write_text(source, encoding='utf-8', newline='\\n')\nvalidator_path.write_text(validator, encoding='utf-8', newline='\\n')\n'@\n  $tempPy = Join-Path $env:RUNNER_TEMP 'issue484_target_modifier_fix_v1.py'\n  Set-Content -LiteralPath $tempPy -Value $patchScript -Encoding utf8\n  python $tempPy\n  if ($LASTEXITCODE -ne 0) { throw 'patch failed' }\n  $changed = @(& git diff --name-only)\n  Write-Host \"CHANGED_FILES=$($changed -join ',')\"\n  if (($changed.Count -ne 2) -or (@($changed | Where-Object { $_ -notin @($sourcePath,$validatorPath) }).Count -ne 0)) { throw \"unexpected changed files: $($changed -join ', ')\" }\n  & git diff --check\n  if ($LASTEXITCODE -ne 0) { throw 'diff-check failed' }\n  $env:PYTHONDONTWRITEBYTECODE = '1'\n  foreach ($name in @('DCOIR_GEMINI_API','DCOIR_OPENAI_API_KEY','DCOIR_OPENAI_PROJECT_ID','OPENAI_API_KEY','OPENROUTER_API_KEY','GEMINI_API_KEY','GOOGLE_API_KEY')) { Remove-Item \"Env:$name\" -ErrorAction SilentlyContinue }\n  python -m py_compile $sourcePath $validatorPath\n  if ($LASTEXITCODE -ne 0) { throw 'py_compile failed' }\n  python $validatorPath\n  if ($LASTEXITCODE -ne 0) { throw 'focused lane-separation regression failed' }\n  $outDir = Join-Path $env:RUNNER_TEMP 'issue484-target-modifier-replay'\n  New-Item -ItemType Directory -Path $outDir -Force | Out-Null\n  python project_sources/gemini/tools/run_gemini_behavioral_replay_validation_suite.py --fixtures-root project_sources/gemini/fixtures/behavioral_replay --output-dir $outDir\n  if ($LASTEXITCODE -ne 0) { throw 'full deterministic Gemini replay failed' }\n  & git config user.name 'github-actions[bot]'\n  & git config user.email '41898282+github-actions[bot]@users.noreply.github.com'\n  & git add -- $sourcePath $validatorPath\n  & git commit -m 'Handle lane target head modifiers'\n  if ($LASTEXITCODE -ne 0) { throw 'commit failed' }\n  $newHead = (& git rev-parse HEAD).Trim()\n  Write-Host \"NEW_HEAD=$newHead\"\n  & git push origin \"HEAD:refs/heads/$branch\"\n  if ($LASTEXITCODE -ne 0) { throw 'push failed' }\n  $remaining = @(& git status --porcelain)\n  if ($remaining.Count -ne 0) { throw \"worktree not clean: $($remaining -join '; ')\" }\n  Write-Host \"ISSUE484_TARGET_MODIFIER_FIX_HEAD=$newHead\"\n} finally {\n  Set-Location $env:GITHUB_WORKSPACE\n  git worktree remove --force $work 2>$null\n}\n"
}
