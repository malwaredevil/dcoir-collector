{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "exec-20260827-pr419-stage8-validation-001",
  "operator_approved": true,
  "approved_at_utc": "2026-08-27T13:34:00Z",
  "shell": "pwsh",
  "timeout_seconds": 1800,
  "artifact_retention_days": 7,
  "cleanup_request_after_run": true,
  "approved_command_preview": "Validate draft PR #419 at exact head d3d7d869a941ad983f194a468ba247f88d2d38c3. Fetch and detach-checkout that head, run the established ten-command agent-runtime validation contract plus the unified release/parity reporter, write a machine-readable summary and parity outputs to the ChatGPT Exec artifact downloads directory, verify the checkout has no tracked diff, and restore the runner to main in finally. Do not mutate the PR branch, workflow YAML, Supabase, live Gemini/OpenAI targets, PR draft state, or merge state.",
  "command": "& { $ErrorActionPreference = 'Stop'; $expected = 'd3d7d869a941ad983f194a468ba247f88d2d38c3'; $branch = 'issue-418-ircore-agent-runtime-alignment'; $repo = $env:DCOIR_REPO_ROOT; $outDir = Join-Path $env:DCOIR_DOWNLOADS_DIR 'pr419-stage8-validation'; New-Item -ItemType Directory -Force -Path $outDir | Out-Null; $commands = @('python project_sources/agent_runtime/tools/validate_shared_agent_source_contract.py','python project_sources/agent_runtime/tests/validate_shared_agent_source_contract_selftest.py','python project_sources/agent_runtime/tools/materialize_agent_behavior_adapters.py --check','python project_sources/agent_runtime/tests/materialize_agent_behavior_adapters_selftest.py','python project_sources/agent_runtime/tools/project_agent_knowledge.py --check','python project_sources/agent_runtime/tests/project_agent_knowledge_selftest.py','python project_sources/agent_runtime/tools/build_openai_dcoir_analyst.py --check','python project_sources/agent_runtime/tests/build_openai_dcoir_analyst_selftest.py','python project_sources/agent_runtime/tools/build_openai_usb_reporting.py --check','python project_sources/agent_runtime/tests/build_openai_usb_reporting_selftest.py'); try { git fetch origin $branch; if ($LASTEXITCODE -ne 0) { throw 'git fetch failed' }; git checkout --detach $expected; if ($LASTEXITCODE -ne 0) { throw 'git checkout failed' }; $observed = (git rev-parse HEAD).Trim(); if ($observed -ne $expected) { throw ('head mismatch: ' + $observed) }; $results = @(); foreach ($cmd in $commands) { Write-Host ('>>> ' + $cmd); & cmd.exe /d /s /c $cmd; $code = $LASTEXITCODE; $results += [pscustomobject]@{ command = $cmd; exit_code = $code }; if ($code -ne 0) { throw ('validation failed: ' + $cmd + ' exit=' + $code) } }; Write-Host '>>> python project_sources/agent_runtime/tools/report_agent_release_parity.py'; & python project_sources/agent_runtime/tools/report_agent_release_parity.py; $reportCode = $LASTEXITCODE; if ($reportCode -ne 0) { throw ('release parity reporter failed exit=' + $reportCode) }; $parityDir = Join-Path $repo 'project_sources/validation/out_agent_release_parity'; if (Test-Path -LiteralPath $parityDir) { Copy-Item -LiteralPath $parityDir -Destination (Join-Path $outDir 'out_agent_release_parity') -Recurse -Force }; git diff --exit-code; if ($LASTEXITCODE -ne 0) { throw 'tracked diff detected after validation' }; $summary = [ordered]@{ schema = 'dcoir.pr419.stage8.validation.v1'; expected_head = $expected; observed_head = $observed; ten_command_contract_passed = $true; ten_command_count = $commands.Count; release_parity_reporter_exit_code = $reportCode; tracked_diff_clean = $true; commands = $results }; $summary | ConvertTo-Json -Depth 6 | Out-File -FilePath (Join-Path $outDir 'validation_summary.json') -Encoding utf8; Write-Host ('validated_head=' + $observed); Write-Host ('ten_command_count=' + $commands.Count); Write-Host 'ten_command_contract_passed=true'; Write-Host 'release_parity_reporter_passed=true'; Write-Host 'tracked_diff_clean=true' } finally { git checkout main | Out-Host; if ($LASTEXITCODE -ne 0) { Write-Warning 'runner restore to main failed' } else { Write-Host 'runner_restored_to_main=true' } } }"
}
