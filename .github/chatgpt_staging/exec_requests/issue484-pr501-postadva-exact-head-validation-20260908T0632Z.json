{
  "schema": "dcoir.chatgpt_staging.exec_request.v1",
  "request_id": "issue484-pr501-postadva-exact-head-validation-20260908T0632Z",
  "operator_approved": true,
  "approved_at_utc": "2026-09-08T06:32:00Z",
  "shell": "powershell_5",
  "timeout_seconds": 1800,
  "artifact_retention_days": 7,
  "cleanup_request_after_run": true,
  "approved_command_preview": "$ErrorActionPreference='Stop'; guard PR #501 branch at 81f628a47cc0f66a2e9ffaffdb9e457695680035; validate only that exact detached head with no inference credentials; py_compile the two changed Python files; run the focused lane-separation regressions and full deterministic Gemini behavioral replay validation suite; verify clean worktree; no source/PR mutation, review invocation, Ready transition, or merge.",
  "command": "$ErrorActionPreference='Stop'; $repo=$env:DCOIR_REPO_ROOT; if (-not $repo) { throw 'DCOIR_REPO_ROOT is not set' }; $branch='issue-484-lane-separation-scoring'; $expected='81f628a47cc0f66a2e9ffaffdb9e457695680035'; $remoteLine=git -C $repo ls-remote origin ('refs/heads/' + $branch); if ($LASTEXITCODE -ne 0) { throw 'git ls-remote failed' }; $remote=(($remoteLine -split '\\s+')[0]); if ($remote -ne $expected) { throw ('PR head drift: expected ' + $expected + ', got ' + $remote) }; $worktree=Join-Path $env:RUNNER_TEMP 'dcoir-issue484-pr501-postadva-exact-head'; if (Test-Path $worktree) { git -C $repo worktree remove --force $worktree 2>$null; Remove-Item -LiteralPath $worktree -Recurse -Force -ErrorAction SilentlyContinue }; git -C $repo fetch --no-tags origin $branch; if ($LASTEXITCODE -ne 0) { throw 'git fetch failed' }; git -C $repo worktree add --detach $worktree $expected; if ($LASTEXITCODE -ne 0) { throw 'git worktree add failed' }; try { Set-Location $worktree; 'OPENROUTER_API_KEY','OPENAI_API_KEY','GEMINI_API_KEY','GOOGLE_API_KEY','DCOIR_GEMINI_API','DCOIR_OPENAI_API_KEY','DCOIR_OPENAI_PROJECT_ID' | ForEach-Object { Remove-Item ('Env:' + $_) -ErrorAction SilentlyContinue }; $env:PYTHONDONTWRITEBYTECODE='1'; python -m py_compile project_sources/gemini/tools/lib/gemini_behavioral_replay_scoring.py project_sources/gemini/tools/validate_gemini_lane_separation_scoring.py; if ($LASTEXITCODE -ne 0) { throw 'py_compile failed' }; python project_sources/gemini/tools/validate_gemini_lane_separation_scoring.py; if ($LASTEXITCODE -ne 0) { throw 'focused lane-separation regression failed' }; $out=Join-Path $env:RUNNER_TEMP 'issue484-gemini-replay-postadva-output'; if (Test-Path $out) { Remove-Item -LiteralPath $out -Recurse -Force }; python project_sources/gemini/tools/run_gemini_behavioral_replay_validation_suite.py --fixtures-root project_sources/gemini/fixtures/behavioral_replay --output-dir $out; if ($LASTEXITCODE -ne 0) { throw 'Gemini behavioral replay validation suite failed' }; $dirty=git status --porcelain; if ($dirty) { throw ('validation dirtied worktree: ' + ($dirty -join '; ')) }; Write-Host 'Issue #484 / PR #501 post-adversarial exact-head validation passed.' } finally { Set-Location $repo; git worktree remove --force $worktree 2>$null }"
}
