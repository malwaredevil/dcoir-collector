name: 98 Temp - PR486 clean precision v3 validation
run-name: PR486 clean precision v3 no-network validation

on:
  push:
    branches:
      - issue-485-first-pass-model-eval-harness
    paths:
      - '.github/workflows/temp-pr486-clean-precision-v3-validation.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Compile and selftest audited precision v3 lane
        run: |
          python3 -m py_compile .github/dcoir_review/scripts/dcoir_review_pr_precision_eval.py .github/dcoir_review/scripts/dcoir_review_pr_precision_eval_selftest.py
          python3 .github/dcoir_review/scripts/dcoir_review_pr_precision_eval_selftest.py
      - name: Verify all ten v3 cases and no-receipt ablation are zero-network
        run: |
          env -u OPENROUTER_API_KEY python3 .github/dcoir_review/scripts/dcoir_review_pr_precision_eval.py --candidate sonnet5-high --output /tmp/precision-v3.json
          DCOIR_PRECISION_INCLUDE_TRUSTED_CONTEXT=0 env -u OPENROUTER_API_KEY python3 .github/dcoir_review/scripts/dcoir_review_pr_precision_eval.py --candidate sonnet5-high --case precision-gha-title-via-env-approved-tested --output /tmp/ablation-v3.json
          python3 - <<'PY'
          import json
          full=json.load(open('/tmp/precision-v3.json'))
          abl=json.load(open('/tmp/ablation-v3.json'))
          assert full['mode']=='plan' and full['planned_requests']==10 and full['network_requests_made']==0
          assert abl['mode']=='plan' and abl['planned_requests']==1 and abl['network_requests_made']==0
          assert 'precision-ps-native-exit-snapshot-verbose-tested' in full['selected_case_ids']
          assert 'precision-py-retry-budget-tested' in full['selected_case_ids']
          assert 'precision-ps-native-exit-snapshot-tested' not in full['selected_case_ids']
          assert 'precision-governance-command-gate-approved-tested' not in full['selected_case_ids']
          print('clean precision v3 plan PASS: 10 audited cases + ablation, zero network')
          PY
